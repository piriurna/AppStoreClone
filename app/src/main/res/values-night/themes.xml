<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.AppStoreClone" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_200</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/black</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_200</item>
        <item name="colorOnSecondary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="drawerArrowStyle">@style/ToolbarIconStyle</item>
    </style>

    <style name="Theme.AppStoreClone.ActionBar" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="android:background">@drawable/light_toolbar_gradient</item>
        <item name="android:elevation">4dp</item>
        <item name="titleTextColor">?attr/colorOnSecondary</item>
        <item name="navigationIconTint">?attr/colorOnSecondary</item>
    </style>

    <style name="Theme.AppStoreClone.ActionBar.Title" parent="TextAppearance.AppCompat.Widget.ActionBar.Title">
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="Theme.AppStoreClone.Title" parent="TextAppearance.AppCompat.Title">
    </style>

    <style name="ToolbarIconStyle" parent="Widget.AppCompat.DrawerArrowToggle">
        <item name="color">?attr/colorOnSecondary</item>
    </style>

    <style name="Theme.AppStoreClone.Fragment">
        <item name="android:background">@color/black</item>
    </style>
</resources>